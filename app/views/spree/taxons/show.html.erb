<div class="taxon">
  <div class="container">
    <%= spree_breadcrumbs(@taxon) %>
    <%= render 'header', taxon: @taxon %>
  </div>
  <%= render 'subcategories',parent_taxon: @taxon.parent, taxon: @taxon %>
  <div data-hook="taxon_products">
    <%= render template: 'spree/products/index', colors: @colors, sizes: @sizes %>
  </div>
</div>
